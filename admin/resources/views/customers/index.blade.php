@extends('layouts.admin', ['title' => 'Customers', 'heading' => 'Customers'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">Relationships</span><h2>Know your customers.</h2><p>Review customer details and order history from WhatsApp enquiries.</p></div></div>
<div class="toolbar"><form class="filter-form" method="GET"><input class="search-input" type="search" name="q" value="{{ request('q') }}" placeholder="Name or phone" aria-label="Search customers"><button class="button button-light" type="submit">Search</button></form><span class="muted">{{ $customers->total() }} customers</span></div>
<section class="panel"><div class="table-scroll"><table class="data-table"><thead><tr><th>Customer</th><th>Phone</th><th>Orders</th><th>Lifetime value</th><th>Last activity</th></tr></thead><tbody>@forelse ($customers as $customer)<tr><td><a class="table-link" href="{{ route('admin.customers.show', $customer) }}">{{ $customer->name }}</a><br><small class="muted">{{ $customer->email ?: 'No email' }}</small></td><td>{{ $customer->phone ?: '—' }}</td><td>{{ $customer->orders_count }}</td><td>${{ number_format((float) ($customer->orders_sum_total ?? 0), 2) }}</td><td>{{ $customer->updated_at->format('d M Y') }}</td></tr>@empty<tr><td class="empty-cell" colspan="5">No customers match this search.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $customers->links() }}</div></section>
@endsection
