@extends('layouts.admin', ['title' => $customer->name, 'heading' => 'Customer detail'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">Customer profile</span><h2>{{ $customer->name }}.</h2><p>{{ $customer->phone ?: 'No phone number saved' }} · {{ $customer->email ?: 'No email saved' }}</p></div><a href="{{ route('admin.customers.index') }}" class="button button-light">← Back to customers</a></div>
<div class="detail-grid"><div class="panel"><div class="panel-heading"><div><span class="eyebrow">Profile</span><h3>Customer details</h3></div></div><dl class="detail-list"><div><dt>Phone</dt><dd>{{ $customer->phone ?: '—' }}</dd></div><div><dt>Email</dt><dd>{{ $customer->email ?: '—' }}</dd></div><div><dt>Address</dt><dd>{{ $customer->address ?: '—' }}</dd></div><div><dt>Notes</dt><dd>{{ $customer->notes ?: '—' }}</dd></div></dl></div><div class="panel"><div class="panel-heading"><div><span class="eyebrow">History</span><h3>Orders</h3></div></div>@forelse ($customer->orders as $order)<div class="attention-row"><div><a class="table-link" href="{{ route('admin.orders.show', $order) }}">{{ $order->order_number }}</a><small>{{ $order->created_at->format('d M Y') }} · {{ $order->status }}</small></div><strong>${{ number_format($order->total, 2) }}</strong></div>@empty<p class="empty-copy">No orders yet.</p>@endforelse</div></div>
@endsection
