@extends('layouts.admin', ['title' => 'Collections', 'heading' => 'Collections'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">Merchandising</span><h2>Curated edits.</h2><p>Shape how customers browse the catalogue, from new arrivals to seasonal stories.</p></div><a href="{{ route('admin.collections.create') }}" class="button button-dark">Add collection <span>＋</span></a></div>
<section class="card-grid">@forelse ($collections as $collection)<article class="collection-card"><div class="collection-image" style="background-image:linear-gradient(0deg,rgba(23,23,23,.5),transparent),url('{{ $collection->image }}')"></div><div class="collection-body"><small>{{ $collection->active ? 'Published' : 'Hidden' }} · {{ $collection->products_count }} products</small><h3>{{ $collection->title }}</h3><p>{{ $collection->subtitle }}</p><div class="form-actions" style="justify-content:flex-start;margin-top:16px;padding-top:14px"><a href="{{ route('admin.collections.edit', $collection) }}" class="text-link">Edit collection ↗</a></div></div></article>@empty<div class="panel"><p class="empty-copy">No collections yet.</p></div>@endforelse</section><div class="pagination-wrap">{{ $collections->links() }}</div>
@endsection
