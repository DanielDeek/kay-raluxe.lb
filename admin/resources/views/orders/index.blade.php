@extends('layouts.admin', ['title' => 'Orders', 'heading' => 'Orders'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">WhatsApp commerce</span><h2>Orders, in one place.</h2><p>Track every enquiry from first message to delivered order.</p></div></div>
<div class="toolbar"><form class="filter-form" method="GET"><input class="search-input" type="search" name="q" value="{{ request('q') }}" placeholder="Order number or customer" aria-label="Search orders"><select class="select-input" name="status" aria-label="Filter by status"><option value="">All statuses</option>@foreach ($statuses as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="button button-light" type="submit">Filter</button></form><span class="muted">{{ $orders->total() }} orders</span></div>
<section class="panel"><div class="table-scroll"><table class="data-table"><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Status</th><th>Payment</th><th class="align-right">Total</th></tr></thead><tbody>@forelse ($orders as $order)<tr><td><a class="table-link" href="{{ route('admin.orders.show', $order) }}">{{ $order->order_number }}</a></td><td><strong>{{ $order->customer_name }}</strong><br><small class="muted">{{ $order->customer_phone }}</small></td><td>{{ $order->created_at->format('d M Y, H:i') }}</td><td><span class="status status-{{ $order->status }}">{{ $order->status }}</span></td><td class="muted">{{ ucfirst($order->payment_status) }}</td><td class="align-right">${{ number_format($order->total, 2) }}</td></tr>@empty<tr><td class="empty-cell" colspan="6">No orders match this filter.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $orders->links() }}</div></section>
@endsection
